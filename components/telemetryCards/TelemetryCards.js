import {
  View,
  Text
} from "react-native";

import {useState, useEffect} from "react"

import config from "../../config"
import TelemetryCardsStyles from "./TelemetryCardsStyles";

export default function TelemetryCards({title, mock}) {
  return (
    <View style={{ }}>
      <Text>Telemtry Cards</Text>
    </View>
  );
}
